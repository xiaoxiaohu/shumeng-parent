spring:
  application:
      name: shumeng-hystrix-dashboard-server
  cloud:
      config:
          uri: http://localhost:8888

eureka:
  instance:
    prefer-ip-address: true

server:
  port: 8022


